# docs/

Project documentation lives here as it accrues. The authoritative working guide
is at the repo root:

- **[../AGENTS.md](../AGENTS.md)** — build/flash, architecture, hardware/protocol
  gotchas, conventions, task map, and milestone roadmap. `CLAUDE.md` defers to it.

Cross-repo references (sibling `connext-emulator`, same parent dir):
- `connext-emulator/docs/FINDINGS-translator.md` — TTGO T8 hardware findings
  (PSRAM split, microSD quirks, early-boot-loop lesson, PSRAM-pin UART conflict).
- `connext-emulator/docs/SPEC-gdl90.md` — GDL90 codec design notes.

Planned docs (per milestone): `FINDINGS.md` (our own hardware log), decoder
porting notes (M1), GPS/AHRS integration notes (M3/M4), weather-store plan (M5).
